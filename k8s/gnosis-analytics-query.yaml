apiVersion: v1
kind: Service
metadata:
  name: gnosis-analytics-query
  namespace: opengnosis
  labels:
    app: gnosis-analytics-query
    tier: backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: gnosis-analytics-query
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gnosis-analytics-query
  namespace: opengnosis
  labels:
    app: gnosis-analytics-query
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gnosis-analytics-query
  template:
    metadata:
      labels:
        app: gnosis-analytics-query
        tier: backend
    spec:
      containers:
      - name: gnosis-analytics-query
        image: opengnosis/gnosis-analytics-query:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: SPRING_PROFILES_ACTIVE
          valueFrom:
            configMapKeyRef:
              name: opengnosis-config
              key: SPRING_PROFILES_ACTIVE
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://$(POSTGRES_HOST):$(POSTGRES_PORT)/$(POSTGRES_DB)
        - name: POSTGRES_HOST
          valueFrom:
            configMapKeyRef:
              name: opengnosis-config
              key: POSTGRES_HOST
        - name: POSTGRES_PORT
          valueFrom:
            configMapKeyRef:
              name: opengnosis-config
              key: POSTGRES_PORT
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: opengnosis-config
              key: POSTGRES_DB
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: opengnosis-secrets
              key: POSTGRES_USER
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: opengnosis-secrets
              key: POSTGRES_PASSWORD
        - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: opengnosis-config
              key: KAFKA_BOOTSTRAP_SERVERS
        - name: SPRING_ELASTICSEARCH_URIS
          valueFrom:
            configMapKeyRef:
              name: opengnosis-config
              key: ELASTICSEARCH_URIS
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      restartPolicy: Always
